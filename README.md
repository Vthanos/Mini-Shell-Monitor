# Mini Shell with System Call Monitoring

This C project implements a basic Unix-like shell. It executes user commands and monitors their memory usage and context switches using information from the /proc filesystem.
